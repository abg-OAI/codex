linux sandbox: Mask daemon mount aliases

A bind-mounted temporary directory can expose the app-server socket
directory through more than one complete path.
Discover every visible directory alias from mount identity and mask each
one after any bind that exposes its backing filesystem.

Continue rejecting mounts of individual sockets, subdirectories, and
nested content because those layouts cannot be isolated by masking the
complete daemon directory.
